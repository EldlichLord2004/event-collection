# CRA (react-scripts) giữ typescript@4; @vercel/analytics khai báo peerOptional cho Remix/Svelte —
# npm strict mode coi đó là xung đột. legacy-peer-deps bỏ qua peerOptional không dùng tới.
legacy-peer-deps=true
